# Knowledge Bridge

Analyzes support ticket patterns to identify documentation gaps and auto-generates help articles. Uses NLP to detect recurring questions not covered in existing knowledge base.

**Stack:** Python 3.11, FastAPI, spaCy, PostgreSQL, Redis

**Run locally:** `python -m uvicorn src.main:app --reload --port 8002`